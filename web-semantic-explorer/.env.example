# Copia este archivo a .env y ajusta los valores si hace falta.
# En un PC nuevo con backup de DB, usa las MISMAS credenciales de Postgres
# que en el equipo donde se generó el backup.

DOMAIN=localhost
FRONTEND_HOST=http://localhost:8080
ENVIRONMENT=local

PROJECT_NAME=EOMExplorer
STACK_NAME=eom-explorer

BACKEND_CORS_ORIGINS="http://localhost,http://localhost:8080,http://localhost:5173"
SECRET_KEY=changethis
FIRST_SUPERUSER=eom@iesteis.es
FIRST_SUPERUSER_PASSWORD=changethis

SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=EOMai
POSTGRES_USER=admin
POSTGRES_PASSWORD=changethis

SENTRY_DSN=

DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend
