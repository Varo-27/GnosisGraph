[project]
name = "root-eom"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.4",
    "sqlmodel>=0.0.38",
]

[tool.uv.workspace]
members = ["scrapper", "web-semantic-explorer/backend"]
